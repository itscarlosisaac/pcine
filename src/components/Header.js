import React from 'react';

class Header extends React.Component{
    render(){
        return(
            <header>
                HEADER COMPONENT
            </header>
        )
    }
}

export default Header;